1/var: var is function scope.
let: let is a blockscope and if you define a variable once you can't apply same name variable again.
const: const also a blockscope variable.if you once declare variable same name you can't decalare variable again.const value is not changeable but in object we can change value by adding some condition.
2/spread: basically spread operator is doing some work like sreading.it can clone objects and arrays data and also marge data from arrays and objects.
3/map: it return a new array.
filter: it can do only some filtering and selecting work.not change the main array.
forEach: it will go every elements of array and do some work without changing the main array
4/arrow function: arrow function is shorhand of function.it is best for short function like make a small decision than we use it.
5/template literals: this is a dynamic string method.with this template liteals we can use data with dynamic way.
